<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MatLog" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:navigationBarDividerColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/main_background_light</item>
        <item name="android:popupMenuStyle">@style/Theme.MatLog.PopupMenu</item>
    </style>

    <style name="Theme.MatLog.Toolbar" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="titleTextColor">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:editTextColor">@android:color/white</item>
    </style>

    <style name="Theme.MatLog.Toolbar.Menu" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:colorBackground">@color/main_background_light</item>
    </style>

    <style name="Theme.MatLog.PopupMenu" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/main_background_light</item>
    </style>
</resources>