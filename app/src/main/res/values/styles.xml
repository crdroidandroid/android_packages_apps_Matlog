<resources>

    <style name="Theme.MatLog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="android:windowLightNavigationBar">?attr/isLightTheme</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/omni_logo_color</item>
    </style>

    <style name="LogcatListItem" parent="@style/TextAppearance.Material3.LabelSmall">
        <item name="android:textSize">@dimen/text_size_medium</item>
        <item name="fontFamily">monospace</item>
    </style>
</resources>
