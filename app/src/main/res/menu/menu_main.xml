<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView" />

    <item
        android:id="@+id/menu_send_log_zip"
        android:alphabeticShortcut="@string/share_log"
        android:icon="@drawable/ic_share_white_24dp"
        android:title="@string/share_log"
        app:showAsAction="always"/>

    <item
        android:id="@+id/menu_filters"
        android:alphabeticShortcut="@string/title_filters"
        android:title="@string/title_filters"
        android:icon="@drawable/ic_filter"
        app:showAsAction="always"/>


    <item
        android:id="@+id/menu_settings"
        android:alphabeticShortcut="@string/settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/settings"
        app:showAsAction="always"/>

</menu>